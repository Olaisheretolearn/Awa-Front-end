# Keep Flutter & plugin classes (usually safe defaults)
-keep class io.flutter.embedding.engine.FlutterJNI { *; }


# Keep your app package (adjust if different)
# -dontwarn com.google.android.play.**
